<!doctype html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head>
    <meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1">
    <title>File de quarantaine</title>
    <style>
        :root{--ink:#202c35;--muted:#687781;--paper:#f4f1ea;--panel:#fffdf8;--line:#d8d5cc;--blue:#24566b;--soft:#e5eef0;--amber:#a76718;--red:#a7463d;--green:#397050;--shadow:0 8px 24px #202c3512}*{box-sizing:border-box}body{margin:0;background:var(--paper);color:var(--ink);font:14px/1.45 "Trebuchet MS",system-ui,sans-serif}body:before{content:"";position:fixed;inset:0;pointer-events:none;opacity:.18;background-image:radial-gradient(#8b877e .6px,transparent .6px);background-size:5px 5px}.shell{max-width:1500px;margin:auto;padding:28px clamp(14px,3vw,44px) 54px;position:relative}.mast{display:flex;justify-content:space-between;align-items:end;gap:20px;border-bottom:1px solid var(--line);padding-bottom:18px;margin-bottom:18px}.eyebrow,th,label{font:700 10px "Courier New",monospace;letter-spacing:.1em;text-transform:uppercase}.eyebrow{color:var(--blue)}h1{font:700 clamp(27px,3vw,40px)/1 Georgia,serif;margin:9px 0 5px}.dek{color:var(--muted);margin:0}.signal{color:var(--muted);font:12px "Courier New",monospace}.signal i{display:inline-block;width:8px;height:8px;border-radius:50%;background:var(--green);margin-right:6px}.toolbar,.queue{background:var(--panel);border:1px solid var(--line);box-shadow:var(--shadow);border-radius:10px}.toolbar{display:grid;grid-template-columns:1.25fr repeat(5,minmax(110px,1fr)) auto;gap:9px;padding:14px;align-items:end}.field label{display:block;color:var(--muted);margin-bottom:5px}.field input,.field select{width:100%;height:38px;background:#fbfaf5;border:1px solid #c8c8bf;border-radius:5px;padding:0 9px;color:var(--ink);font:inherit}.actions{display:flex;gap:7px}.btn{display:inline-flex;align-items:center;justify-content:center;min-height:38px;padding:0 13px;border-radius:5px;border:1px solid var(--blue);background:var(--blue);color:#fff;text-decoration:none;font-weight:700;cursor:pointer;font:700 13px inherit;white-space:nowrap}.btn.ghost{background:transparent;color:var(--blue)}.btn:hover{filter:brightness(1.07);transform:translateY(-1px)}:focus-visible{outline:3px solid #d8a44c;outline-offset:2px}.summary{display:flex;justify-content:space-between;gap:15px;margin:19px 2px 10px}.summary strong{font:700 17px Georgia,serif}.muted{color:var(--muted);font-size:12px}.table-wrap{overflow-x:auto}table{width:100%;min-width:980px;border-collapse:collapse;table-layout:fixed}th,td{padding:12px 11px;border-bottom:1px solid var(--line);text-align:left;vertical-align:top;overflow-wrap:anywhere}th{background:#f8f6f0;color:var(--muted);white-space:nowrap}th:nth-child(1){width:5%}th:nth-child(2){width:22%}th:nth-child(3){width:9%}th:nth-child(4){width:18%}th:nth-child(5){width:14%}th:nth-child(6){width:9%}th:nth-child(7){width:11%}th:nth-child(8){width:12%}tbody tr:hover td{background:#faf8f2}tr:last-child td{border-bottom:0}.mono{font:12px "Courier New",monospace}.copy{color:var(--ink);font-weight:700;text-decoration:none}.copy:hover{color:var(--blue);text-decoration:underline}.sub{display:block;color:var(--muted);font-size:12px;margin-top:3px}.compact{display:inline-flex;gap:6px;flex-wrap:wrap;margin-top:5px}.metric{padding:2px 6px;border-radius:3px;background:var(--soft);color:var(--blue);font:700 10px "Courier New",monospace;white-space:nowrap}.badge{display:inline-block;padding:4px 8px;border-radius:4px;font:700 10px "Courier New",monospace;white-space:nowrap}.badge.red{background:#f6e3df;color:var(--red)}.badge.amber{background:#f7ead1;color:var(--amber)}.badge.green{background:#e1eee5;color:var(--green)}.fifo{font:700 18px Georgia,serif;color:var(--blue)}.action-cell{position:sticky;right:0;background:var(--panel);box-shadow:-8px 0 12px -12px #202c3566;text-align:right;white-space:nowrap}.action-cell .btn{width:100%;padding-inline:9px}.empty{text-align:center;color:var(--muted);padding:58px}.empty strong{display:block;color:var(--ink);font:22px Georgia,serif;margin-bottom:5px}.pagination{display:flex;flex-wrap:wrap;gap:6px;padding:13px;border-top:1px solid var(--line)}.sr-only{position:absolute;width:1px;height:1px;padding:0;margin:-1px;overflow:hidden;clip:rect(0,0,0,0)}@media(max-width:1100px){.toolbar{grid-template-columns:repeat(3,1fr)}.toolbar .field:first-child,.toolbar .actions{grid-column:1/-1}}@media(max-width:620px){.mast{align-items:start;flex-direction:column}.toolbar{grid-template-columns:1fr}.toolbar .field:first-child,.toolbar .actions{grid-column:auto}.actions .btn{flex:1}.summary{align-items:start;flex-direction:column}}
    </style>
</head>
<body>
<main class="shell">
    <header class="mast"><div><div class="eyebrow">Admin / Intégrité du contenu</div><h1>File de quarantaine</h1><p class="dek">Revue FIFO des noyaux nécessitant une décision experte avant la prochaine phase.</p></div><div class="signal"><i></i> canal de revue opérationnel</div></header>
    @if(session('status'))<div class="badge green" role="status">{{ session('status') }}</div>@endif
    @if($errors->quarantine->any())<div class="badge red" role="alert">{{ $errors->quarantine->first() }}</div>@endif
    <form class="toolbar" method="GET" action="{{ url('/admin/questions/quarantine') }}" aria-label="Filter quarantine queue">
        <div class="field"><label for="blueprint">Blueprint</label><input id="blueprint" name="blueprint" value="{{ $filters['blueprint'] ?? '' }}" placeholder="Identifiant blueprint"></div>
        <div class="field"><label for="state">État</label><select id="state" name="state"><option value="">Tous</option>@foreach(['EDITABLE','READY','IN_FLIGHT'] as $value)<option value="{{ $value }}" @selected(($filters['state'] ?? '') === $value)>{{ $value }}</option>@endforeach</select></div>
        <div class="field"><label for="cause">Cause</label><select id="cause" name="cause"><option value="">Toutes</option>@foreach(['SUSPICION','CONTENT_UNTRANSLATABLE','MANUAL_CORRECTION'] as $value)<option value="{{ $value }}" @selected(($filters['cause'] ?? '') === $value)>{{ str_replace('_',' ', $value) }}</option>@endforeach</select></div>
        <div class="field"><label for="phase">Phase d’origine</label><select id="phase" name="phase"><option value="">Toutes</option>@foreach(['PHASE1','VALIDATION_PHASE1','PHASE2','VALIDATION_PHASE2'] as $value)<option value="{{ $value }}" @selected(($filters['phase'] ?? '') === $value)>{{ str_replace('_',' ', $value) }}</option>@endforeach</select></div>
        <div class="field"><label for="slot">Type cognitif</label><input id="slot" name="slot" value="{{ $filters['slot'] ?? '' }}" placeholder="Code du slot"></div>
        <div class="field"><label for="language">Langue</label><select id="language" name="language"><option value="">Toutes</option>@foreach(['fr','es','de','it','pt','ru','zh','ar','el'] as $value)<option value="{{ $value }}" @selected(($filters['language'] ?? '') === $value)>{{ strtoupper($value) }}</option>@endforeach</select></div>
        <div class="actions"><button class="btn" type="submit">Filtrer</button><a class="btn ghost" href="{{ url('/admin/questions/quarantine') }}">Réinitialiser</a></div>
    </form>
    <div class="summary"><div><strong>{{ $rows->total() }} {{ $rows->total() === 1 ? 'copie' : 'copies' }}</strong> <span class="muted">· ordre FIFO</span></div><span class="muted">Affichage {{ $rows->firstItem() ?: 0 }}–{{ $rows->lastItem() ?: 0 }}</span></div>
    <section class="queue"><div class="table-wrap"><table><thead><tr><th>FIFO</th><th>Noyau / copie</th><th>État</th><th>Origine / propriétaire</th><th>Cause</th><th>Couleur</th><th>Révisions</th><th class="action-cell"><span class="sr-only">Action</span></th></tr></thead><tbody>
    @forelse($rows as $row)
        @php
            $tone = $row['color'] === 'GREEN'
                ? 'green'
                : ($row['color'] === 'YELLOW' ? 'amber' : 'red');
            $slotCount = count(array_filter(array_map('trim', explode(',', $row['cognitive_type']))));
            $languageCount = count($row['available_languages']);
        @endphp
        <tr><td><span class="fifo">{{ $row['fifo_position'] ?? '—' }}</span></td><td><a class="copy" href="{{ url('/admin/questions/quarantine/'.$row['copy_id']) }}">{{ $row['kernel_code'] ?: $row['copy_id'] }}</a><span class="sub">{{ $row['blueprint_id'] }} · v{{ $row['copy_version'] }}</span><span class="compact"><span class="metric">{{ $slotCount }} slots</span><span class="metric">{{ $languageCount }} langues</span></span></td><td><span class="badge {{ $row['state'] === 'READY' ? 'green' : ($row['state'] === 'EDITABLE' ? 'amber' : 'red') }}">{{ $row['state'] }}</span></td><td><span class="mono">{{ $row['origin_phase'] }}</span><span class="sub">propriétaire : {{ $row['owner_phase'] }}</span></td><td>{{ str_replace('_',' ', $row['cause_code'] ?: '—') }}</td><td><span class="badge {{ $tone }}">{{ $row['color'] }}</span></td><td class="mono">manuelle {{ $row['manual_revision'] }}<br>slot {{ $row['slot_revision'] }}</td><td class="action-cell"><a class="btn ghost" href="{{ url('/admin/questions/quarantine/'.$row['copy_id']) }}">Examiner</a></td></tr>
    @empty<tr><td colspan="8"><div class="empty"><strong>La file est vide.</strong>Aucune copie ne correspond aux filtres.</div></td></tr>@endforelse
    </tbody></table></div>@if($rows->hasPages())<div class="pagination">{!! $rows->links() !!}</div>@endif</section>
</main>
</body>
</html>