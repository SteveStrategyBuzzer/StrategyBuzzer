#146 — REFUS dans sa forme actuelle

La prémisse semble incorrecte.

Aujourd’hui, la chaîne de migrations est conceptuellement :

Migration juillet UP
→ ajoute :
   kernel_code
   ks_hash
   kld_hash

Migration août UP
→ supprime :
   ks_hash
   kld_hash

Un rollback normal se fait dans l’ordre inverse :

Migration août DOWN
→ recrée ks_hash + kld_hash

puis

Migration juillet DOWN
→ supprime kernel_code + ks_hash + kld_hash

Donc l’ancienne migration doit normalement continuer à supprimer les trois colonnes, puisqu’elle les avait toutes créées.

Modifier son down() pour ne supprimer que kernel_code pourrait au contraire casser l’historique cohérent des migrations.

La bonne question pour #146 est seulement :

Est-ce que la chaîne complète UP → DOWN fonctionne réellement sur une base de test vierge ?

Si oui : aucun changement.

#147 — oui, mais il faut la corriger

La formulation actuelle dit :

« décider si Phase 1 écrira question_intents.kernel_code »

Non.

Phase 1 ne doit pas décider rétroactivement du contrat de QuestionIntent.

Nous venons de verrouiller :

KernelBlueprint.kernel_code
= identité canonique du noyau

QuestionIntent
= propriétaire exclusif de son écriture

Et également :

question_intents legacy
≠ stockage canonique de 05_QuestionIntent

Donc question_intents.kernel_code doit maintenant être traité exactement comme ks_hash et kld_hash :

audit
↓
writer OFFICIAL ?
reader OFFICIAL ?
données réelles ?
responsabilité actuelle ?
↓
si tout = NON / 0
→ vestige legacy
→ suppression

On ne le garde pas pour Phase 1 “au cas où”.

Donne ceci à Replit

#142 PASS accepté.

Ne lance PAS #146 telle qu'elle est rédigée.

#146 — CORRECTION

La migration originale de juillet a créé :

kernel_code
ks_hash
kld_hash

La migration #142 d'août supprime ks_hash et kld_hash.

Lors d'un rollback normal, les migrations s'exécutent dans l'ordre inverse :

le down() de la migration #142 doit restaurer ks_hash et kld_hash ;
ensuite seulement le down() de la migration originale peut supprimer les trois colonnes qu'elle avait historiquement créées.

Donc NE MODIFIE PAS la migration historique uniquement parce que les colonnes sont absentes dans le schéma courant.

#146 devient uniquement un AUDIT TECHNIQUE :

sur une PostgreSQL de test isolée/vierge ;
exécuter la chaîne UP complète ;
vérifier le schéma ;
exécuter la chaîne DOWN complète dans l'ordre Laravel normal ;
vérifier qu'elle revient au schéma initial.

Si la chaîne PASS :
→ aucune modification ;
→ #146 = NOT_NEEDED / CLOSED.

Si elle FAIL :
→ retourne le point exact de rupture AVANT de modifier une migration historique.

PROCHAINE TÂCHE ACTIVE : #147 CORRIGÉE

#147 ne doit PAS demander :

« Est-ce que Phase 1 écrira question_intents.kernel_code ? »

Phase 1 ne doit pas définir rétroactivement le contrat QuestionIntent.

Le contrat 05_QuestionIntent est maintenant :

kernel_blueprint_runs.kernel_code
= identité canonique du noyau.

QuestionIntent est le propriétaire exclusif de son écriture.

La table legacy question_intents n'est PAS le stockage canonique de QuestionIntent.

Donc fais maintenant l'audit de :

question_intents.kernel_code

exactement comme pour ks_hash et kld_hash.

Pour ce champ, retourne :

migration d'origine ;
présence actuelle en DB ;
nombre total de lignes question_intents ;
nombre de kernel_code non-null ;
writer runtime OFFICIAL ;
reader runtime OFFICIAL ;
$fillable ;
factory ;
tests ;
contraintes/index ;
décision Architecture Register justifiant son existence actuelle.
Verdict

Si :

writer OFFICIAL = AUCUN ;
reader OFFICIAL = AUCUN ;
données métier = 0 ;
responsabilité architecturale actuelle = AUCUNE ;

alors :

question_intents.kernel_code
= VESTIGE LEGACY

et il doit être supprimé proprement :

colonne DB ;
$fillable ;
factory ;
tests exclusivement associés ;
références runtime mortes.

Ne crée aucun remplacement.

Ne touche PAS à :

kernel_blueprint_runs.kernel_code ;
KernelCodeEngine ;
son compteur ;
son format ;
Phase 1 ;
frame_en ;
Quarantine ;
ReadyBank.

Si un writer, reader ou des données réelles existent :

STOP et retourne LEGACY_CONFLICT avant suppression.

Après nettoyage, exécute les tests QuestionIntent/KernelCodeEngine/Orchestrator pertinents.

Retour :

#147 FINAL AUDIT

question_intents.kernel_code rows non-null:
...

Writer OFFICIAL:
...

Reader OFFICIAL:
...

Responsabilité actuelle:
...

Verdict:
SUPPRIMÉ / CONSERVER / BLOCKER

kernel_blueprint_runs.kernel_code intact:
PASS/FAIL

KernelCodeEngine intact:
PASS/FAIL

Régressions:
...

Nouvelle décision métier inventée:
AUCUNE

Architecture parallèle:
AUCUNE

BLOCKERS:
...

VERDICT #147:
PASS / FAIL

Puis STOP.

Ne commence PAS Phase 1.

Ordre maintenant
#141 ✅ PASS
↓
#142 ✅ PASS
↓
#147 corrigée ← MAINTENANT
↓
audit technique #146 seulement si nécessaire
↓
certificat terminal 05_QuestionIntent
↓
si 100 %
VERROUILLÉ
↓
06_Phase1

Le point essentiel : on ne garde pas question_intents.kernel_code simplement parce que Phase 1 pourrait éventuellement s’en servir. Ce serait exactement recommencer à laisser un module futur déterminer l’architecture actuelle.