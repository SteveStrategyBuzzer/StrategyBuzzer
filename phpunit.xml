<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         processIsolation="false"
         stopOnFailure="false"
         cacheDirectory=".phpunit.cache"
         backupGlobals="false">
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_DRIVER" value="array"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <!-- CRITICAL: tests must NEVER touch the live Postgres DB.
             RefreshDatabase runs migrate:fresh which drops every table.
             We force sqlite in-memory here so tests are sandboxed.
             force="true" est OBLIGATOIRE : sans lui, PHPUnit n'écrase PAS une
             variable déjà présente dans l'environnement réel du process. Comme
             DB_CONNECTION=pgsql et DATABASE_URL existent en runtime, l'override
             serait ignoré et les tests retomberaient sur Neon. -->
        <env name="DB_CONNECTION" value="sqlite" force="true"/>
        <env name="DB_DATABASE" value=":memory:" force="true"/>
        <!-- CRITICAL: neutralise DATABASE_URL pour que la connexion sqlite ne soit
             jamais détournée vers la base Postgres Neon pendant les tests. -->
        <env name="DATABASE_URL" value="" force="true"/>
        <!-- CRITICAL: Laravel lit env() via $_SERVER EN PREMIER. Comme DB_CONNECTION
             et DATABASE_URL existent dans l'environnement réel du process (injectés
             par Replit), ils sont présents dans $_SERVER et l'emportent sur les <env>
             ci-dessus. On force donc AUSSI les superglobales $_SERVER, sinon les
             tests retomberaient sur la base Postgres Neon de production. -->
        <server name="DB_CONNECTION" value="sqlite" force="true"/>
        <server name="DB_DATABASE" value=":memory:" force="true"/>
        <server name="DATABASE_URL" value="" force="true"/>
    </php>
</phpunit>
