> **ARCHIVE — NE PAS UTILISER COMME SOURCE ACTIVE.** 05_QuestionIntent brides supersédées. Conservé uniquement pour la traçabilité historique.

# 05_QuestionIntent — Brides actives avant reconstruction

**Statut :** À RECONSTRUIRE / SPÉCIFIER DANS L’ORDRE OFFICIEL  
**Nature du fichier :** brides déjà connues ; pas une spécification complète.

## Brides connues

- QuestionIntent intervient après Taxonomy dans le pipeline.
- QuestionIntent est le propriétaire fonctionnel de `kernel_code`.
- `KernelCodeEngine` est le mécanisme d’implantation utilisé pour cette responsabilité.
- QuestionIntent lit le territoire nécessaire déjà inscrit dans le Blueprint.
- QuestionIntent écrit `kernel_code` dans le Blueprint.

## Interdictions connues

- ne modifie pas `depth + domain` ;
- ne remplace pas le triplet Taxonomy ;
- ne possède pas les SubjectBanks / IdeaBanks Taxonomy.